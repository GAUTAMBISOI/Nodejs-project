<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TASK CREATION USING NODE JS</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <div class="formm p-10">
            <form  action="/check" method="post">
                <input class="block px-5 py-2 w-full bg-zinc-800   rounded-md" type="text" placeholder="Enter the Task" name="task">
                <textarea class="block px-5 py-2 w-full bg-zinc-800  rounded-md mt-2 " placeholder="Enter the text Field" name="detail"></textarea>
                <input class="px-5 mt-2 mx-1 py-2 rounded-md bg-blue-500 text-white text-xl font-semibold" type="submit">
            </form>
        </div>
       <div class="tasks p-10 flex flex-wrap gap-5 ">
        <% if(files.length>0){%>
                <% files.forEach(function(val){ %>
              <div class="task bg-zinc-800 rounded-md w-[20vw] px-5 py-2 mb-5">
               <h1 class="text-3xl tracking-tighter font-semibold"><%= val%></h1>
               <div class="flex items-center justify-between">
                   <a class="text-blue-500 tracking-tighter text-sm" href="/file/<%=val%>"> Read more</a>
                   <a class="text-gray-500 tracking-tighter text-sm" href="/edit/<%=val%>">edit</a>
               </div>
              </div>
            <% })%>
        <%}else{ %>
        <h1 class="text-3xl font-semibold tracking-tighter text-zinc-600"> task not here.</h1>
        <% }%>
    
         
       </div>
    </div>
</body>
</html>